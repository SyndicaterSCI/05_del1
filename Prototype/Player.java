public class Player {

    // Should make a integer that is "Totalscore"
public int = totalscore;


//Should generate a string that makes the user input a name
Player(String name){
    this.name = name;
}

    String getName() {
        return name;
    }

    // Should get the totalscore from the "Game" and then display it to the player
    int getTotalScore() {
        return totalScore;
    }

    // Calculate the score, not return value
    void setTotalScore(int score) {
        totalScore += score;
    }
}
